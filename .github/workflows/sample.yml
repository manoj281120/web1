name: sample
on:
  push:
    branches: ["main"]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
     - name: Checkout code
       uses: actions/checkout@v3
     - name: deploying a file to GitHub pages
       uses: peaceiris/actions-gh-pages@v3
       with:
         github_token: ${{secrets.urkeyy}}
         publish_dir: .
     
         
   
